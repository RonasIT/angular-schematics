{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsSharedModuleSection",
  "title": "Shared Module Section Schema",
  "type": "object",
  "properties": {
    "type": {
      "description": "The type of shared module.",
      "type": "string",
      "enum": [
        "component",
        "directive",
        "pipe",
        "service"
      ]
    },
    "name": {
      "description": "The name of the shared module.",
      "type": "string",
      "minLength": 1
    },
    "intoSection": {
      "type": "boolean",
      "description": "When true, generates shared module into section."
    },
    "section": {
      "type": "string",
      "description": "The name of the section.",
      "x-prompt": "What is the name of the section?"
    },
    "parentPage": {
      "type": "string",
      "description": "The name of the parent page."
    },
    "intoPage": {
      "type": "boolean",
      "description": "When true, generates shared module into page.",
      "x-prompt": "Would you like to generate this shared module into page?"
    },
    "page": {
      "type": "string",
      "description": "The name of the page."
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the shared module.",
      "visible": false
    }
  },
  "required": [
    "name",
    "type",
    "intoSection",
    "section"
  ]
}