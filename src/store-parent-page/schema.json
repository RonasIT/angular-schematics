{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsParentPageStore",
  "title": "Store Parent Page Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the shared module.",
      "type": "string"
    },
    "intoSection": {
      "type": "boolean",
      "description": "When true, generates store into section."
    },
    "section": {
      "description": "The name of the section.",
      "type": "string"
    },
    "intoParentPage": {
      "type": "boolean",
      "description": "When true, generates store into parent page."
    },
    "parent": {
      "description": "The name of the parent page.",
      "type": "string",
      "x-prompt": "What is the name of the parent page?"
    },
    "intoPage": {
      "type": "boolean",
      "description": "When true, generates store into page."
    },
    "page": {
      "description": "The name of the page.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the store.",
      "visible": false
    }
  },
  "required": [
    "intoSection",
    "intoPage",
    "intoParentPage",
    "section",
    "page",
    "parent"
  ]
}